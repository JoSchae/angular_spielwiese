import { Theme } from './symbols';

export const mandantOneTheme: Theme = {
    name: 'mandantOne',
    properties: {
        '--override-title': 'red',
        '--override-background-color': 'black'
    }
};
