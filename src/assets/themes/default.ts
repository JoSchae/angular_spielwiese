import { Theme } from './symbols';

export const defaultTheme: Theme = {
    name: 'default',
    properties: {
        '--override-title': 'blue',
        '--override-background-color': 'orange'
    }
};
